const noop = () => {};
describe.skip('Gist tests', () => {
  it('should render a description, fork count and file count', noop)
  it('should toggle on click', noop)
  it('should show a panel when expanded', noop)
  it('should render filetypes and tabs when expanded', noop)
  it('should handle an empty object or property gracefully', noop)
})