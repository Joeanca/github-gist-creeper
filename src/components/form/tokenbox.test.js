const noop = () => {};
describe.skip('TokenBox tests', () => {
  it('should render with props/children passed in', noop)
  it('should be able to read/set the token via context', noop)
  it('should update token if context changed', noop)
  it('should have the accordion contracted if token is set', noop)
  it('should have the accordion expanded if token not of certain length', noop)
  it('should link to the correct location on helper click', noop)
  it('should update the token context on button click', noop)
  it('should read different accordion title text if the token is of certain length, or not', noop)
  it('should update state on user type', noop)
  it('should handle an empty object or property gracefully', noop)
})