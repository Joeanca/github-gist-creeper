const noop = () => {};
describe.skip('GistTabs tests', () => {
  it('should render however many tabs exist in the props to display', noop)
  it('should render a GithubAvatar for each tab available', noop)
  it('should render a different GithubUserAvatar with each tab click', noop)
  it('should handle an empty object or property gracefully', noop)
})