const noop = () => {};
describe.skip('LandingLayout tests', () => {
  it('should render with props/children passed in', noop)
  it('should render a title', noop)
  it('should handle an empty object or property gracefully', noop)
})