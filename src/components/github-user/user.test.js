const noop = () => {};
describe.skip('GithubUser tests', () => {
  it('should render with props passed in', noop)
  it('should show an alternative avatar url if url not found', noop)
  it('should render a bio prop if something other than text passed in', noop)
})